# Copia este archivo como .env y rellena con tus datos reales
# cp .env.example .env

# ── Base de datos MySQL ──
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=tu_password_aqui
DB_NAME=immersive_aguachica

# ── JWT ──
JWT_SECRET=cambia_esta_clave_por_algo_seguro_2025

# ── Puerto del servidor ──
PORT=3000
